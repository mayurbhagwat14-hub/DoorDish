/**
 * Product feature flags.
 * Set DINING_ENABLED to true to restore table-booking / dining UI + routes.
 * When false: no dining tabs, sidebar links, or booking entry points.
 * Backend dining APIs stay mounted but unused by the UI.
 */
export const DINING_ENABLED = false;
